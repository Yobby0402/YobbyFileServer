# 🚀 快速开始指南

> 3 分钟快速上手 Yobboy 文件服务器  
> **当前版本：R1 (Release V1) · 发布日期 2026-01-13**

---

## 📥 第一步：下载程序

### 方式一：从 GitHub Releases 下载（推荐）
1. 访问 [GitHub Releases](https://github.com/Yobby0402/YobbyFileServer/releases)
2. 下载最新版本的压缩包
3. 解压
4. 双击运行即可

### 方式二：下载完整项目
1. 从 [GitHub 仓库](https://github.com/Yobby0402/YobbyFileServer) 下载 ZIP 包
2. 解压到任意目录
3. 配置环境，运行main.py

---

## ⚙️ 第二步：首次配置

1. **双击运行** `YobboyFileServer.exe`

2. **设置根目录和密码**
   - 首次启动请先点击"文件"菜单 → "设置"
   - 点击"浏览"按钮选择文件根目录（建议选择一个常用的工作目录）
   - 设置登录密码（默认：`password`，建议立即修改）
   - 设置管理员密码（用于保护目录修改等重要操作）
   - 配置服务器端口（可选，默认：5000）
   - 选择关闭行为（退出程序或最小化到托盘）
   - 点击"保存设置"

---

## 🎯 第三步：启动服务器

1. **启动服务器**
   - 点击主界面的 **"🟢 启动服务器"** 按钮
   - 等待提示"服务器启动成功"
   - 点击下方任一地址可以使用

2. **看到选择界面**
   - **文件浏览器** - 管理和预览文件，支持多格式预览和在线编辑
   - **ToDo 任务管理** - 项目任务管理和进度跟踪，支持Excel导出和汇报视图
   - **产品对比** - 产品参数对比和雷达图可视化分析
   - **Draw.io 编辑器** - 创建和编辑流程图、架构图等
   - **串口调试助手** - 串口实时调试工具，支持本地和远程模式

---

## 📋 使用 ToDo 任务管理

1. 点击选择界面的 **"ToDo 任务管理"** 卡片进入任务管理界面
2. **搜索和筛选**：
   - 搜索框支持按项目名称、任务简述、描述搜索
   - 项目筛选下拉框可选择特定项目或查看全部项目
3. **项目管理**：
   - 点击 **"新建项目"** 按钮创建新项目，设置项目名称和主题色
   - 项目表格默认折叠，点击项目头部展开查看任务列表
   - 项目头部显示：项目名称、创建时间、任务数、整体进度（进度条+百分比）
4. **任务管理**：
   - 在项目表格中，点击 **"新建任务"** 按钮创建任务
   - 填写任务简述、详细描述、优先级（1-5）、预计完成时间、进度等信息
   - 任务支持拖拽排序：点击序号列拖拽即可调整任务顺序
   - 长描述自动折叠，点击"展开"查看完整内容
5. **任务操作**：
   - **编辑任务**：点击任务行的编辑按钮，修改任务信息
   - **添加评论**：点击评论按钮，记录任务进展或补充说明
   - **删除任务**：点击删除按钮，确认后删除任务
6. **待完成概览**：
   - 页面底部以卡片方式展示待完成任务
   - 按逾期、今日、即将到期、未设置分类显示
   - 卡片左侧颜色条与项目主题色保持一致
   - 点击卡片可跳转到对应任务

---

## 📊 使用产品对比工具

1. 点击选择界面的 **"产品对比"** 卡片进入产品对比工作台

2. **创建对比文件**：
   - 点击"新建产品对比"按钮
   - 输入对比文件名称（例如："无线充电接收器件对比"）
   - 点击确认创建

3. **定义属性列表**：
   - 在"属性管理"标签页中，系统已默认创建"品名"和"归属"两个属性
   - 点击"添加属性"按钮，添加需要对比的属性（如：价格、充电电流、大小等）
   - 对于需要数值对比的属性，勾选"通用参数"复选框
   - 为通用参数设置单位（如：元、毫安、平方毫米）和方向（越大越好/越小越好）

4. **添加产品**：
   - 点击"添加产品"按钮
   - 填写产品品名和归属（必填）
   - 为归属选择颜色（可选择已有归属的颜色或自定义颜色，支持随机生成）
   - 填写其他属性的值（通用参数需要输入数值）
   - 保存后产品会出现在左侧列表中，左侧书签显示归属颜色

5. **查看产品详情**：
   - 点击左侧产品列表中的产品名称
   - 在"产品详情"标签页中查看该产品的所有属性值

6. **雷达图对比**：
   - 在左侧产品列表中勾选要对比的产品（支持单选或多选）
   - 切换到"雷达图"标签页
   - 系统自动生成雷达图，不同产品用不同颜色的多边形表示
   - 查看下方的对比分析，了解每个参数的最优产品和各产品的最优项统计

7. **参数排序**：
   - 切换到"参数排序"标签页
   - 选择要排序的通用参数
   - 选择排序方式（从大到小/从小到大）
   - 查看排序结果表格

8. **管理对比文件**：
   - 使用左上角的文件选择器切换不同的对比文件
   - 可以创建多个对比文件，分别管理不同的产品对比场景
   - 支持删除不需要的对比文件

---

---

## 🔌 使用串口调试助手

1. 在选择界面点击 **"串口调试助手"** 或访问 `/serial_tool`
2. **本地模式**：在支持 Web Serial 的 Chromium 浏览器下直接授权串口，支持气泡/终端双显示模式、快捷键、定时发送
3. **远程模式**：通过 Socket.IO 连接服务器串口，可在浏览器远程调试嵌入式设备
4. **持续日志**：在左侧“持续日志”面板选择串口和参数，点击“开启持续日志”即可后台写入 JSONL，随时载入历史记录而不影响实时调试
5. 若需要查阅日志，可在面板中点击“查看日志”或通过 API `GET /api/serial/capture/log/<port_id>` 获取

---

## 📂 使用文件浏览器

### 浏览文件
1. 点击"文件浏览器"进入
2. 点击文件夹进入子目录
3. 点击面包屑导航快速返回上级目录
4. 点击文件名查看预览

### 预览文件
- **图片文件** (`.jpg`, `.png`, `.gif`, `.svg` 等)：点击直接预览
- **Markdown 文件** (`.md`)：点击查看渲染后的内容（支持表格、代码高亮等）
- **Draw.io 文件** (`.drawio`, `.dio`)：点击查看图表预览
- **3D模型** (`.gltf`, `.glb`, `.obj`, `.stl`)：点击查看3D模型（v2.2新增）
- **PDF 文件**：点击在浏览器中预览
- **视频文件** (`.mp4`, `.mov` 等)：点击在线播放
- **音频文件** (`.mp3`, `.wav`, `.flac` 等)：点击在线播放
- **Office 文件** (`.docx`, `.xlsx`, `.pptx`)：仅支持下载

### 新建和上传

**新建内容：**
1. 点击右上角 **"新建"** 下拉按钮
2. 选择要创建的类型：
   - **新建文件** - 创建空文本文件
   - **新建Draw.io图表** - 创建并打开图表编辑器
   - **新建文件夹** - 创建目录
3. 输入名称，点击创建

**上传文件：**
1. 点击右上角 **"上传"** 按钮
2. 选择一个或多个文件（按住Ctrl多选）
3. 点击"开始上传"
4. 文件将上传到当前目录

### ✏️ 编辑文件
1. **点击文件名**查看预览
2. **点击标题栏的"编辑文件"按钮**进入编辑模式
3. **开始编辑**：
   - 自动语法高亮（支持30+种语言）
   - 行号、代码折叠、括号匹配
   - 使用Ctrl+F查找，Ctrl+H替换
4. **保存文件**：
   - **保存**：Ctrl+S 保存继续编辑
   - **保存并关闭**：保存后自动返回预览
   - **取消**：ESC 放弃更改

**支持的文件类型（100+种）**：
- **Web开发**: html, css, scss, less, js, jsx, ts, tsx, vue, php
- **编程语言**: python, java, c, cpp, cs, go, rust, ruby, swift, kotlin, perl, lua, haskell, erlang
- **脚本**: shell, bash, bat, powershell
- **标记语言**: markdown, rst, latex, textile
- **数据格式**: json, xml, yaml, toml, protobuf
- **数据库**: sql, mysql, postgresql
- **配置文件**: nginx, dockerfile, ini, conf, properties
- **模板引擎**: jinja2, django, handlebars, pug
- **其他**: diff, git, http, verilog, vhdl, dockerfile 等

### 管理文件
- **下载**：点击文件旁的下载按钮
- **分享**：点击分享按钮创建分享链接
- **删除**：点击删除按钮（需要确认）
- **上传**：使用上传按钮添加文件到当前目录

---

## 🎨 使用 Draw.io 编辑器

### 创建新图表
1. 在主页点击"Draw.io 编辑器"
2. 开始绘制图表
3. 按 **Ctrl+S** 保存
4. 输入文件名（例如：`流程图.drawio`）
5. 文件自动保存到服务器

### 编辑已有图表
1. 在文件浏览器中找到 `.drawio` 文件
2. 点击文件名进入编辑模式
3. 修改后按 **Ctrl+S** 保存
4. 自动更新服务器上的文件

### 下载图表
1. 点击编辑器顶部的"下载到本地"
2. 文件会下载到您的下载文件夹

### 导出图表
- 支持导出为PNG、JPEG、SVG等格式
- 点击"文件" → "导出"选择格式

---

## 💡 常用技巧

### 快捷键
- **Ctrl+S**：保存文件（Draw.io / 代码编辑器）
- **Ctrl+F**：查找内容（代码编辑器）
- **Ctrl+H**：替换内容（代码编辑器）
- **ESC**：退出编辑模式
- **双击托盘图标**：显示/隐藏主窗口

### 系统托盘
- **最小化到托盘**：点击主窗口的"最小化到托盘"按钮
- **恢复窗口**：双击系统托盘图标
- **右键菜单**：
  - 显示主窗口
  - 隐藏到托盘
  - 启动/停止服务器
  - 退出程序

### 访问地址
- **本地访问**：`http://localhost:5000`
- **局域网访问**：`http://[您的IP地址]:5000`
  - 查看 IP 地址：在主窗口日志中可以看到
  - 例如：`http://192.168.1.100:5000`

---

## 🛠️ 高级配置

### 修改配置文件
编辑 `config.ini` 文件（与 exe 同目录或用户目录下的 `.yobboy_file_server` 文件夹）：

```ini
[settings]
# 文件服务器根目录
root_dir = /path/to/your/files

# 登录密码（默认：password）
password = your_password

# 管理员密码（默认：admin123）
admin_password = admin123

# 关闭按钮行为
# true = 最小化到托盘，false = 退出程序
close_to_tray = false

# 服务器端口（默认：5000，范围：1-65535）
port = 5000
```

**注意**：修改配置后需要重启服务器才能生效

### 更改根目录
1. **方式一（推荐）**：在主窗口菜单栏点击"文件" → "设置"，通过 GUI 修改
2. **方式二**：直接编辑 `config.ini` 中的 `root_dir`（修改后需重启服务器）

### 设置关闭行为和端口
1. 打开"设置"对话框
2. 勾选或取消勾选"点击关闭按钮时最小化到托盘"
3. 保存设置
4. 立即生效，无需重启

---

## ❓ 常见问题

### 无法启动服务器？
- 检查端口是否被占用（默认5000，可在设置中修改）
- 尝试更改端口：在GUI设置中修改端口，或编辑 `config.ini`，改为 `port = 8080`
- 查看日志窗口的错误信息

### 无法访问网页？
- 检查防火墙是否阻止了端口
- 尝试使用 `127.0.0.1:5000` 而不是 `localhost:5000`
- 确认服务器已成功启动

### Draw.io 保存失败？
- 确保文件路径有写入权限
- 使用 **Ctrl+S** 而不是右下角按钮
- 文件名不要包含特殊字符

### 托盘图标不显示？
- 确保 `文件服务器.png` 或 `文件服务器.ico` 与 exe 在同一目录
- 检查系统托盘是否被隐藏
- 重启程序

### Markdown 渲染异常？
- 确保文件使用 UTF-8 编码
- 图片路径使用相对路径
- 检查 Markdown 语法是否正确

### 代码编辑器打开但内容为空？
- 清除浏览器缓存（Ctrl+Shift+R）
- 检查文件编码是否为UTF-8或GBK
- 查看浏览器控制台（F12）的错误信息

### 编辑后保存失败？
- 确保已登录
- 检查文件路径权限
- 查看服务器日志中的错误信息

---

## 📞 获取帮助

### 文档
- 📖 [完整 README](README.md) - 详细项目说明
- 📝 [更新日志](CHANGELOG.md) - 版本历史

### 技术支持
- 🐛 [提交 Bug](https://github.com/Yobby0402/YobboyFileServer/issues/new?labels=bug)
- 💡 [功能建议](https://github.com/Yobby0402/YobboyFileServer/issues/new?labels=enhancement)
- 💬 [讨论区](https://github.com/Yobby0402/YobboyFileServer/discussions)

---

## 🎉 开始使用

现在您已经准备好了！

1. ✅ 启动服务器
2. ✅ 打开浏览器
3. ✅ 选择文件浏览器或 Draw.io
4. ✅ 开始工作！

---

<div align="center">

**祝您使用愉快！**

Made with ❤️ by Yobboy

</div>



# 更新日志

## R1.4-dev (2026-04-08)

### 🛠 体验优化（ToDo / 产品对比）

- 汇报视图在粘土拟态主题下恢复表格行选中高亮（与拟态渐变背景正确叠加）。
- 汇报视图支持在「项目名称」或「任务简述」单元格上右键关闭「显示在汇报页」：经浏览器确认框确认后，调用接口写入项目/任务的 `show_in_report` 并持久化；可在编辑项目/任务中重新勾选恢复。
- 「导出汇报 Excel」「全屏查看」移至顶部工具栏（已归档项目按钮之后），仅在汇报视图显示；导出按钮文案为「导出汇报 Excel」，导出内容与当前汇报表一致（仅含 `show_in_report` 为真的条目）。
- 产品对比页在玻璃/粘土拟态下强化勾选框的选中样式（主题色底 + 对勾），与未选状态区分更明显。

### ✨ 新增功能

**本地 AI + MCP 集成**
- 新增本地 AI 对话能力，支持通用/知识库/代办模式，以及流式输出与手动停止生成
- 后端新增本地 MCP 桥接（`local_mcp_bridge.py`），本地 AI 可通过 MCP 工具统一调用待办与知识库能力
- 新增 MCP 调用记录面板（ToDo AI 与 Draw.io AI），可追踪每次工具调用和结果

**ToDo AI 变更确认链路**
- 新增待办变更可视化操作编辑器，操作类型与项目/任务引用改为人类可读展示
- 新增“校验 JSON”流程（`/api/local-ai/todo/validate`），支持二次确认和纠错
- 待确认操作支持本地持久化；关闭弹窗或刷新后可恢复，悬浮 AI 按钮红色闪烁提示待执行操作
- 变更应用成功后触发页面刷新事件，解决“已应用但列表未更新”的问题

**Draw.io AI（Phase 1 + Phase 2）**
- 新增 `drawio_validate_xml`、`drawio_summarize_xml`、`drawio_diff_summary` 三个校验/摘要工具
- 新增 `drawio_repair_xml` 自动修复工具，并接入失败后重试反馈循环
- 增加生成过程进度事件与 MCP 事件输出，便于前端展示诊断信息

### 🛠 优化改进

- 优化 Draw.io 重试上下文裁剪策略，减少长上下文导致的模型失败
- 增加最终收口校验（repair + validate），确保 summarize/diff 与最终应用使用稳定 XML
- 增加布局质量检测项（`LAYOUT_OVERLAP`、`LAYOUT_OUT_OF_BOUNDS`），提升图形可读性

### 🐛 问题修复

- 修复待办字段（如 `conclusion`、`weekly_plan`、`task_type`）被误判为评论导致更新失败的问题
- 修复关闭确认弹窗后待执行操作丢失的问题
- 修复刷新页面后 MCP 相关记录与对话状态显示不一致的问题
- 修复 Draw.io 在多轮修复后仍出现最终 `xml_parse_error` 的收口失败场景

---

## R1.3 (2026-04-02)

### ✨ 新增功能

**串口助手 - 远程共享与统一接入**
- 新增共享串口中枢：浏览器页面可直接选择本地串口并共享到服务器，无需额外客户端程序，其他设备在“远程串口”列表中即可直接连接
- 统一服务器串口（PySerial）与浏览器共享串口（Web Serial）的远程入口，支持多客户端同时查看、同时发送，并实时同步收发内容
- 新增共享串口历史存储与分页加载机制：默认只加载最新一段记录，更多历史按需从文件中继续读取，避免长时间会话导致页面卡顿
- 优化串口占用策略：默认不长期占用远程串口，支持断开、休眠与手动停止持续日志；浏览器共享串口在拥有者断开后自动下线

**串口助手 - HTTPS 与离线可用性**
- 支持远程串口 HTTPS 兼容模式，可在保留主站 HTTP 的同时，为串口助手单独提供 HTTPS 入口，满足 Web Serial 安全上下文要求
- 内置本地 `socket.io.min.js`，不再依赖外部 CDN，离线环境和打包后的 EXE 均可直接使用

**Git 集成**
- 新增 GUI 内托管 SSH 密钥生成能力，可直接生成兼容性更高的 RSA 密钥、查看公钥并回填配置
- 改进 Git 连接配置与 SSH 认证处理，完整 URL、默认配置密钥和错误提示的配合更清晰

**ToDo v2**
- 项目支持归档状态，归档项目不再进入待完成概览，便于保留历史项目同时维持主视图清爽
- 任务新增“本周计划”字段，并参与持久化、更新历史和导出流程

### 🛠 优化改进

- 新增主题首屏引导片段，页面在首帧即应用主题色与拟态风格，显著减少文件服务器等页面的背景闪烁与拟态延迟
- 串口页终端模式、共享串口列表、历史日志查看与持续日志控制体验统一，支持跨客户端接管服务器端持续日志
- `build_exe.py` 增强打包校验，补充共享串口与本地 Socket.IO 相关静态资源检查

### 🐛 问题修复

- 修复串口页在部分环境下提示 `socket.io not loaded`，导致远程串口和本地串口页面初始化失败的问题
- 修复远程共享串口未正确显示到列表、终端模式不可用等串口页回归问题
- 修复文件服务器页面在进入子目录或执行操作时，背景与拟态样式短暂回退到默认状态的闪烁问题

---

## R1.2.1 (2026-01-29)

### 🐛 问题修复

**串口助手与持续日志**
- 修复持续日志开启失败「module 'serial' has no attribute 'Serial'」：当系统中存在冲突的 `serial` 包或 pyserial 的 `serial/__init__.py` 缺失时，改为从 `serial.serialwin32`（Windows）/ `serial.serialposix`（Linux）直接导入 `Serial`，保证持续日志可正常使用
- 修复串口助手「安全上下文」提示中的 localhost 链接端口固定为 5000 的问题：改为使用当前页面端口 `window.location.port`，修改端口后点击链接可正确跳转
- 修复持续日志「启动时间」显示比实际快 8 小时：`serial_manager` 中 `started_at` 不再追加 `Z`（UTC 后缀），按本地时间存储与显示

**串口助手 - 持续日志体验**
- 持续日志列表增加「记录时长」「记录长度」显示，并每 3 秒自动刷新
- 增加「弹窗查看」按钮，可在弹窗中查看当前端口日志内容并支持刷新
- 统一「弹窗查看」「加载到监控区」「连接」「停止」按钮样式与文案，避免混淆

**产品对比**
- 修复选择或打开对比文件后，顶部文件选择框仍显示「请选择」的问题：在 `loadFileList` 完成后根据 `currentFileId` 恢复选中项，正确显示当前文件名

**ToDo v2**
- 修复待完成概览行为：未设置截止日期的任务不再在待完成概览区域展示卡片（仅逾期/今日/即将到期任务显示卡片），「未设置截止」数量仍正常统计显示

---

## R1.1 (2026-01-13)

### ✨ 新增功能

**📋 ToDo 任务管理系统增强**
- **汇报表格Excel导出**：
  - 支持将汇报视图导出为Excel文件
  - 自动根据当前页面列宽调整Excel列宽
  - 智能行高调整，根据内容自动计算行高，确保内容完整显示
  - 所有内容垂直居中，支持自动换行
  - Excel表格支持筛选功能，可直接在Excel中使用筛选
- **会议记录（笔记）功能**：
  - 独立的笔记管理功能，支持按日期创建和管理笔记
  - Markdown编辑和实时预览（左右分屏模式）
  - 笔记列表支持搜索、排序和筛选
  - 支持搜索笔记内容，自动匹配并定位到对应笔记
  - 支持按日期或更新时间排序
  - 历史笔记默认折叠，点击展开查看

**🔒 完全离线化**
- **marked.js本地化**：将marked.js从CDN迁移到本地，完全移除外部依赖
- 所有JavaScript库均本地化，确保完全离线运行

### 🔧 技术改进

- 优化Excel导出行高计算算法，更准确地根据内容计算所需行数
- 改进列宽转换算法，更准确地从像素转换为Excel列宽单位
- 笔记搜索功能支持异步加载和匹配，提升搜索效率
- 笔记列表支持实时筛选和排序，无需重新加载

### 🐛 问题修复

- 修复Excel导出行高不足导致内容显示不完整的问题
- 修复笔记搜索功能无法搜索内容的问题
- 修复笔记模态框Bootstrap初始化错误

---

## R1 (Release V1) (2026-01-13)

### 🎉 正式发布版本

这是 Yobboy 文件服务器的第一个正式发布版本（R1），整合了所有开发版本的功能，并进行了全面的优化和文档完善。

### ✨ 主要特性

**🔧 配置管理增强**
- **端口配置**：在GUI设置界面中添加了服务器端口配置功能（1-65535）
- **默认密码更新**：将默认登录密码从 `ats123` 更改为 `password`，提高安全性
- **配置同步**：所有配置项（根目录、密码、端口、关闭行为）统一在GUI中管理

**🎨 主题系统统一**
- **20种精美主题**：统一的CSS变量系统，支持实时切换
- **全站主题同步**：所有页面（选择界面、登录界面、todo、串口工具、drawio、产品对比等）统一应用主题
- **动态背景**：选择界面支持动态渐变背景，根据主题自动调整颜色
- **按钮样式统一**：所有按钮使用主题变量，确保配色一致

**📚 文档完善**
- **功能说明文档**：新增完整的功能说明文档（`功能说明.md`），详细描述所有功能模块
- **帮助页面更新**：同步更新帮助界面，包含产品对比工具等新功能说明
- **README更新**：更新版本信息，将历史版本标记为dev开发版本
- **快速开始指南**：更新配置步骤，包含端口配置说明

**🧹 代码清理**
- **移除旧版功能**：完全删除旧版ToDo界面（`templates/todo.html`）及相关路由
- **代码优化**：统一主题变量使用，移除硬编码颜色
- **性能优化**：服务器启动优化，IP地址缓存，减少启动时间

### 🔧 技术改进

- 在 `SettingsDialog` 中添加端口配置输入框和验证
- 更新 `load_or_create_config()` 和 `save_config()` 支持端口配置
- 统一所有页面的主题系统，使用 `var(--t-*)` CSS变量
- 更新 `theme_floater.js` 支持20种主题
- 优化服务器启动流程，添加IP缓存机制

### 🐛 问题修复

- 修复选择界面主题不统一的问题
- 修复登录界面主题切换不生效的问题
- 修复Draw.io和串口工具界面的主题不一致问题
- 修复服务器启动延迟问题（IP地址查询优化）

### 📝 文档更新

- 新增 `功能说明.md` - 完整的功能说明文档
- 更新 `README.md` - 版本信息、默认密码、端口配置
- 更新 `快速开始.md` - 配置步骤、端口说明
- 更新 `templates/help.html` - 帮助页面内容同步

---

## v3.2 (2025-12-03) [dev]

### ✨ 全新特性

**📊 ToDo v2 Excel 导出功能**
- 支持将任务数据导出为 Excel 文件，提供灵活的导出配置选项
- **时间范围选择**：支持导出全部、仅已完成、仅待完成或自定义时间范围的任务
- **列选择与排序**：可选择导出的列，支持拖拽调整列顺序
- **项目导出方式**：
  - 方式一：所有项目导出到单个 Sheet，添加"项目名称"列作为第一列
  - 方式二：每个项目导出到独立 Sheet，Sheet 名称为项目名称
- **评论处理方式**：
  - 方式一：评论内联显示，在任务行的评论列中显示多行评论（时间戳+内容）
  - 方式二：评论单独 Sheet，任务行添加超链接跳转到评论 Sheet
- **导出当前预览**：一键导出当前表格显示的内容和顺序，只应用评论处理方式设置
- **自定义文件名**：支持自定义导出文件名，未设置则使用默认命名规则

**🎨 ToDo v2 界面优化**
- **双风格切换**：合并卷轴风格和表格风格，通过滑动开关一键切换
  - 卷轴风格：保留原有的项目卡片展示方式
  - 表格风格：全新的表格预览界面，支持列拖拽排序
- **改动数量显示**：将"评论数"改为"改动数量"，显示"X次修改 Y条评论"
- **历史记录查看**：统一的改动和评论查看弹窗，支持筛选"仅修改"、"仅评论"或"全部"
- **表格筛选与排序**：
  - 支持按项目名称、创建时间、预计完成时间、最后更新时间进行筛选
  - 支持按创建时间、预计完成时间、最后更新时间、进度、优先级进行排序
  - 每个列可单独设置筛选条件（日期范围筛选）
  - 同时只能有一个筛选和一个排序处于激活状态
- **字体大小调整**：使用滑块控制字体大小（10-48px），统一应用到显示区域和待完成卡片
- **可折叠待完成概览**：待完成任务列表支持折叠/展开，节省界面空间
- **表格交替行背景**：表格风格添加交替行背景色，便于区分不同行

### 🔧 技术改进
- 新增 `/api/todo/v2/export/excel` API 接口，使用 `openpyxl` 生成 Excel 文件
- 优化 `todo_v2.js`，合并卷轴和表格风格的渲染逻辑
- 添加 `todo_v2_export.js` 模块，处理 Excel 导出配置
- 优化表格渲染性能，支持大量数据的流畅展示
- 改进数据持久化，保存用户界面偏好（风格、字体大小、列顺序等）

### 🐛 问题修复
- 修复 Excel 导出时超链接不显示的问题
- 修复表格风格中表头和内容错行的问题
- 修复上传按钮图标和文字换行显示问题
- 修复初始化时界面不显示内容的问题
- 优化 Excel 导出格式，添加交替行颜色和单元格边框

---

## v3.1 (2025-01-XX)

### ✨ 全新特性

**📋 新版ToDo任务管理系统**
- 完全重构的ToDo系统，采用项目-任务层级结构，替代原有的时间轴设计
- **项目表格展示**：
  - 可折叠的项目表格，每个项目显示主题色背景、项目名称、创建时间、整体进度（进度条+百分比）
  - 项目表格默认折叠，点击展开查看任务列表
  - 项目整体进度自动计算（所有任务进度的平均值）
- **任务列表管理**：
  - 表格形式展示任务，包含：序号、简述、详细描述、创建时间、预计完成、优先级、进度、上次更新
  - 详细描述独立列显示，长描述自动折叠，点击展开查看完整内容
  - 任务支持拖拽重新排序，灵活调整任务优先级
  - 优先级1-5级，不同颜色标识（灰色/蓝色/绿色/橙色/红色）
  - 预计完成时间带状态标识：逾期（红色）、今日（黄色）、即将到期（蓝色）
- **更新历史记录**：
  - 每次任务更新都会记录时间和变更内容
  - 更新任务基本属性后，显示立即更新
  - 完整追踪任务的所有变更历史
- **评论系统**：为任务添加评论，记录最新进展或补充说明
- **待完成概览**：
  - 卡片方式展示待完成任务，替代原有的列表方式
  - 按逾期、今日、即将到期、未设置分类展示
  - 卡片左侧颜色条与项目主题色保持一致
  - 点击卡片可跳转到对应任务
- **搜索和过滤**：
  - 搜索框和筛选框同一行显示，与按钮对齐
  - 支持按项目名称、任务简述、描述搜索
  - 支持按项目筛选
- **完整CRUD操作**：
  - 新建/编辑/删除项目
  - 新建/编辑/删除任务
  - 添加/删除评论
  - 任务拖拽排序
- **数据持久化**：
  - 使用新的数据格式（`data/todos_v2.json`）
  - 与旧版数据文件（`todos.json`）完全分离
  - 保留旧版ToDo入口，方便访问历史数据

### 🔧 技术改进
- 重构 `todo_manager.py`，采用新的数据结构和API
- 新增 `/api/todo/v2/*` 系列API接口
- 新增 `templates/todo_v2.html`、`static/css/todo_v2.css`、`static/js/todo_v2.js`
- 更新 `templates/choice.html`，默认入口指向新版ToDo界面
- 优化打包流程，确保资源文件正确复制到dist目录

### 🐛 问题修复
- 修复详细描述首行空白问题
- 优化搜索框和筛选框布局，确保同一行显示
- 修复待完成概览卡片颜色显示问题

---

## v4.0 (2025-11-19)

### ✨ 全新特性

**📊 产品对比工具**
- 全新的产品参数对比功能，支持多产品管理和可视化对比
- **属性管理**：灵活定义产品属性，支持默认属性（品名、归属）和自定义属性
- **通用参数配置**：标记为通用参数的属性支持数值输入，可设置单位和方向（越大越好/越小越好）
- **归属分类**：产品按归属自动分类，每个归属可自定义颜色，产品列表左侧书签显示归属颜色
- **雷达图可视化**：基于 Chart.js 的雷达图，支持多产品叠加对比
  - 每个参数独立归一化，基于选中产品的最大最小值动态缩放
  - 支持单产品或多产品对比，不同产品用不同颜色区分
  - 多边形叠加显示，确保所有产品都可见
  - 雷达图中心从 -10 开始，避免最差产品聚集到中心点
- **智能分析**：
  - 自动识别每个参数的最优产品（根据方向判断）
  - 统计各产品的最优项数量
  - 生成详细的对比分析报告
- **参数排序**：按任意通用参数对产品进行排序，支持升序/降序切换
- **数据管理**：支持创建多个对比文件，每个文件独立管理产品和属性
- **数据持久化**：所有数据保存在本地 JSON 文件（`data/product_compare/`），支持文件级别的增删改查
- **茉莉飘雪主题**：产品对比页面采用统一的茉莉飘雪主题风格

### 🔧 技术改进
- 新增 `product_compare_manager.py` 模块，负责产品对比数据的 CRUD 操作
- 新增产品对比相关的 API 路由（文件、属性、产品、归属管理）
- 集成 Chart.js 库用于雷达图生成（离线版本）
- 优化串口模块导入，修复打包时的 `SerialException` 导入错误

### 🐛 问题修复
- 修复串口模块在打包后的导入错误（`SerialException`）
- 优化打包配置，添加 `serial.serialutil` 等隐藏导入

---

## v3.0 (2025-11-11)

### ✨ 全新特性

**🍃 茉莉奶白主题**
- 全站（除选择页）升级为 Jasmine Green × Snow White 渐变主题
- 统一按钮、卡片、表单、提示等组件样式
- 新增浮动上下滚动按钮，支持平滑跳转板块

**🗂️ ToDo 时间轴中心**
- 新增独立 ToDo 工作台，支持后门直达、搜索与项目筛选
- 项目颜色自动继承，可随机生成；新增预计完成时间字段
- 新建/编辑/评论功能整合到时间轴卡片，操作更直观

**📆 双向时间轴与记录管理**
- 时间轴支持纵向与横向两种视图，滚轮自动匹配方向
- 时间轴卡片支持评论、修改、单条记录删除；历史与评论同步更新
- 新增待完成概览板块：今日目标、逾期、剩余时间排序一屏展示

**🔌 串口助手增强**
- 支持为指定串口开启后台持续日志，日志以 JSONL 格式写入，可在网页端加载回放
- 浏览器端可直接接入日志中的串口，与实时调试互不干扰

### 🔧 体验优化
- 时间轴滚动体验升级，添加 scroll-snap 与流畅动画
- 删除时间轴记录时同步更新 ToDo 数据与评论列表
- 调整页面布局，让 ToDo 时间轴与待办概览自适应屏幕高度

---

## v2.1 (2025-11-04)

### ✨ 新增功能

**✏️ 在线代码编辑器**
- 集成CodeMirror专业编辑器
- 支持30+种编程语言的语法高亮
- 直接在预览区域编辑，无需跳转页面
- Monokai暗色主题，护眼专业

**🎨 编辑器功能**
- 行号显示
- 代码折叠
- 括号自动匹配和补全
- 查找替换（Ctrl+F / Ctrl+H）
- 支持Ctrl+S快捷键保存
- ESC快捷键取消编辑

**💾 智能保存**
- 保存：保存后继续编辑
- 保存并关闭：保存后自动返回预览
- 取消：放弃更改并返回预览
- 未保存更改时会提示确认

**📝 支持的文件类型（100+种语言）**
- Web开发: html, css, scss, less, javascript, typescript, jsx, vue, php
- 编程语言: python, java, c, cpp, csharp, go, rust, ruby, swift, kotlin, perl, lua, haskell, erlang, elixir, clojure
- 脚本语言: shell, bash, batch, powershell
- 标记语言: markdown, restructuredtext, latex, textile
- 数据格式: json, xml, yaml, toml, protobuf
- 数据库: sql, mysql, postgresql, cypher
- 配置文件: nginx, dockerfile, ini, conf, properties
- 模板引擎: jinja2, django, handlebars, pug, smarty, velocity
- 其他: diff, git, http, verilog, vhdl, cmake, makefile 等

### 🔧 技术改进
- 新增 `/get_file_content` API用于获取文件内容
- 优化编辑器布局，完美占满预览区域
- 支持UTF-8和GBK编码自动检测
- 添加详细的调试日志

### 🎯 用户体验优化
- 编辑按钮显示在预览标题栏（类似drawio）
- 编辑和预览无缝切换
- 保存后自动刷新预览内容
- 优雅的通知提示动画

---

## v2.0 (2025-11-03)

### 🎉 重大更新

#### 新增功能

**🔍 智能搜索与过滤**
- 实时搜索文件名
- 按类型过滤（全部、文件夹、图片、文档、视频、音频、代码）
- 搜索和过滤可组合使用

**🔗 分享链接系统**
- 一键生成分享链接
- 密码保护（可选）
- 过期时间设置（1小时~30天）
- 访问次数限制
- 二维码自动生成
- 访问统计和记录
- Drawio/Markdown文件在线预览

**🎵 多媒体增强**
- 音频播放器（MP3, WAV, FLAC, OGG, WMA, M4A, AAC）
- 图片灯箱效果（点击图片全屏查看）
- 预览区域全屏功能
- 视频播放器增强

**🔒 安全增强**
- 管理员密码保护
- 修改目录需要管理员密码验证
- 双重密码系统（登录密码 + 管理员密码）

**🌐 网络优化**
- 智能识别真实网卡IP
- 自动排除虚拟网络接口（VMware/VirtualBox/Docker）
- 优先使用可访问的局域网IP

#### 优化改进

**🎨 界面优化**
- 统一所有页面字体大小（0.95rem-1.8rem）
- 优化分享页面布局和排版
- 修复图标显示问题（Font Awesome）
- 改进按钮样式和间距
- 响应式设计优化

**🐛 问题修复**
- 修复Windows控制台编码错误
- 修复Drawio分享预览问题
- 修复预览页面header被挤压问题
- 删除无用的测试文件

#### 技术改进

**新增文件**
- `share_links.py` - 分享链接核心模块
- `share_links.db` - SQLite数据库（自动创建）
- 8个分享相关模板页面

**更新文件**
- `main.py` - 添加管理员密码、智能IP选择
- `routes.py` - 添加分享路由、音频支持
- `templates/index.html` - 添加搜索、过滤、灯箱、全屏
- `.gitignore` - 添加数据库文件忽略

---

## v1.0 (2025-10-30)

### 初始版本

- 文件浏览和下载
- Markdown渲染
- Draw.io集成
- 密码保护
- 系统托盘支持

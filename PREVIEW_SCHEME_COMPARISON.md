# 预览功能实现方案对比

## 方案一：当前实现（后端生成JSON + SessionStorage传递）

### 实现方式
1. 用户点击"预览"按钮
2. 前端收集导出配置，调用后端API `/api/todo/v2/export/preview`
3. 后端复用导出Excel的数据准备逻辑，返回JSON格式的预览数据
4. 前端将数据存储到SessionStorage，生成唯一key
5. 打开预览页面，通过URL参数传递dataKey
6. 预览页面从SessionStorage读取数据，使用HTML表格渲染
7. 可选：使用ExcelJS在浏览器端生成Excel文件

### 优点
1. ✅ **数据一致性高**：完全复用后端导出逻辑，预览和导出的数据完全一致
2. ✅ **实时性强**：每次预览都是最新数据，不会过期
3. ✅ **服务器负担小**：数据已在后端准备好，前端只负责渲染
4. ✅ **实现简单**：代码逻辑清晰，易于维护
5. ✅ **支持复杂逻辑**：后端可以处理复杂的业务逻辑（如周报筛选、日期计算等）
6. ✅ **安全性好**：数据通过SessionStorage传递，不会暴露在URL中
7. ✅ **无需文件管理**：不需要管理临时JSON文件

### 缺点
1. ❌ **每次预览都需要请求后端**：网络请求次数较多
2. ❌ **数据量大时可能较慢**：如果任务很多，JSON数据可能较大
3. ❌ **SessionStorage限制**：理论上存在大小限制（通常5-10MB）

---

## 方案二：URL参数传递配置 + 前端加载JSON文件

### 实现方式
1. 用户点击"预览"按钮
2. 前端收集导出配置，封装为URL参数（或编码为base64）
3. 打开预览页面，URL包含：
   - JSON文件路径（如：`/data/todos_v2.json`）
   - 导出配置参数（如：`exportRange=weekly&includeCompleted=true&...`）
4. 预览页面加载时：
   - 从URL读取JSON文件路径和配置参数
   - 前端通过fetch加载JSON文件
   - 前端根据配置参数处理数据（过滤、格式化等）
   - 使用ExcelJS生成Excel并在浏览器中预览

### 优点
1. ✅ **可分享性强**：URL可以分享，其他人打开URL即可看到相同的预览
2. ✅ **减少后端请求**：配置通过URL传递，不需要SessionStorage
3. ✅ **前端处理灵活**：可以在前端进行更多定制化处理
4. ✅ **支持离线查看**：如果JSON文件已缓存，可以离线查看（需要PWA支持）

### 缺点
1. ❌ **数据可能过期**：如果JSON文件更新了，预览的数据可能不是最新的
2. ❌ **实现复杂度高**：需要在前端实现数据筛选、格式化等逻辑，与后端逻辑可能不一致
3. ❌ **URL长度限制**：如果配置参数很多，URL可能会很长（超过浏览器限制）
4. ❌ **安全性问题**：文件路径和配置暴露在URL中，可能被恶意使用
5. ❌ **前端逻辑重复**：需要在前端重新实现数据准备逻辑，容易出现不一致
6. ❌ **性能问题**：大数据量时，前端处理可能较慢
7. ❌ **文件路径问题**：需要确保JSON文件路径可以通过HTTP访问
8. ❌ **配置参数编码**：复杂的配置参数需要编码，可能不够直观

---

## 详细对比表

| 对比项 | 方案一（当前实现） | 方案二（URL+JSON） |
|--------|-------------------|-------------------|
| **数据实时性** | ✅ 总是最新数据 | ❌ 可能过期 |
| **数据一致性** | ✅ 完全一致 | ⚠️ 可能不一致 |
| **实现复杂度** | ✅ 简单 | ❌ 复杂 |
| **代码维护性** | ✅ 容易维护 | ❌ 逻辑分散 |
| **可分享性** | ❌ URL不能分享（包含session key） | ✅ URL可以分享 |
| **网络请求** | ⚠️ 每次预览需要请求 | ✅ 只需加载JSON文件 |
| **安全性** | ✅ 较安全 | ⚠️ 文件路径暴露 |
| **性能** | ✅ 后端处理，性能好 | ⚠️ 前端处理，大数据可能慢 |
| **扩展性** | ✅ 容易扩展新功能 | ⚠️ 需要在前后端都实现 |
| **错误处理** | ✅ 后端统一处理 | ⚠️ 前端需要处理各种错误 |
| **调试难度** | ✅ 容易调试 | ❌ 前后端逻辑都需要调试 |

---

## 推荐方案

**推荐使用方案一（当前实现）**，理由如下：

1. **数据一致性最重要**：预览的目的是让用户确认导出结果，必须与导出结果完全一致
2. **维护成本低**：逻辑集中在后端，只需要维护一套代码
3. **实时性保证**：总是显示最新数据，不会出现数据过期问题
4. **安全性好**：不暴露文件路径，配置不暴露在URL中

### 方案二的适用场景

如果未来有以下需求，可以考虑方案二：
- 需要支持预览URL分享功能
- 需要支持离线预览（配合PWA）
- 需要支持大量并发预览（减少服务器负担）

但在这些场景下，建议采用混合方案：
- 后端API生成预览数据并返回URL token
- 预览URL包含token而不是完整配置
- 后端缓存预览数据，token有效期较短
- 这样既保证了数据一致性，又支持了URL分享

---

## 总结

当前方案（方案一）更适合预览功能的核心需求：**准确、实时、易维护**。方案二虽然在某些特定场景下有优势，但带来的复杂度和潜在问题更多，不适合作为主要方案。

